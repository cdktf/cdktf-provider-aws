# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
name: ProviderUpgrade
on:
  schedule:
    - cron: 0 */4 * * *
  workflow_dispatch: {}
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 10.17.0
      - run: npm install @cdktf/provider-project@0.0.34
      - run: npx projen@0.3.37
      - run: yarn install --frozen-lockfile
      - name: Anti-tamper check
        run: git diff --exit-code
      - run: yarn fetch
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          commit-message: "chore: upgrade provider"
          branch: auto/provider-upgrade
          title: "chore: upgrade provider"
          body: This PR upgrades provider to the latest version
